package task5and6;

public class ConvertToDecimal {

	public static void main(String[] args) {
		int num=234; int base=7;
		System.out.println(convertToDecimal(num, base));

	}

	private static int convertToDecimal(int number, int base){
		StringBuilder num=new StringBuilder();
		num.append(number).reverse();
		System.out.println(num.toString());
		int result=0;
		for (int i = 0; i < num.length(); i++) {	
			System.out.println(num.charAt(i));
			int sum=(int) Math.pow(base, i) *num.charAt(i) ;System.out.println(p);
			result+=sum;			
		}
			
		return result;
		
	}
}
