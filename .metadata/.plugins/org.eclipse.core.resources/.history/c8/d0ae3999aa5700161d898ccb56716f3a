
public class Problem58 {

    public static void main(String[] args) {
        int noOfPrimes = 3;
        int sl = 2;
        int c = 9;
         
        while( ((double)noOfPrimes)/(2*sl+1) > 0.10){
            sl += 2;
            for(int i = 0; i < 3; i++){
                c += sl;
                if(isPrime(c)) noOfPrimes++;
            }
            c+= sl;
        }
        System.out.println(sl+1);
        //top-right: n^2 
        //top-left: n^2 - n + 1 
        //bottom-left: n^2 - 2n + 2 
        //bottom-right: n^2 - 3n + 3
        int n=3, all=13;
        int countOfPrimes=8; double ratio=0;
 
        while (ratio > 0.1 ) {
            countOfPrimes+=prime(n * n)+prime(n * n - n + 1)+prime(n * n - 2 * n + 2)+prime(n * n - 3 * n + 3);
            n++; all+=4;
            ratio=countOfPrimes/all;
        }
        System.out.println(all/4+2);
    }

    private static int prime(long number) {
        return isPrime(number) ? 1 : 0;
    }
    
    private static boolean isPrime(long number){
        if (number < 2) {
            return false;
        }
        double lim=Math.sqrt(number);
        for (int i = 2; i <= lim; i++) {
            if (number % i == 0) {
                return false;
            }
        }
        return true;
    }
}
